# Algo-Trader
utomated stock trading bot built in Python. Connects to Alpaca's brokerage API to detect market signal changes using statistical models and machine learning, then executes trades instantly. Features a full data pipeline, SQLite database, risk management controls, and paper trading mode. Currently in active development.
